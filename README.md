# AI-Resume-Reviewer-with-OpenAI
An intelligent resume evaluation system built using **LangChain**, **OpenAI GPT models**, and **Flask** that analyzes a candidate's resume against a job description and generates a structured, human-readable HTML report.
